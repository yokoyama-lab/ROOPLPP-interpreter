; 警告は表示するが致命的にはしない。2026-08-04 に警告はゼロにしたので
; 「既存の警告を通すため」ではなく、**新しいコンパイラが増やした警告で
; 第三者のビルドが壊れないため**にこの設定を残している。
; 新しい警告が入り込んでいないことは CI が検査する（.github/workflows/ci.yml
; の "the build must stay warning-free"）。
(env
 (_
  (flags (:standard -warn-error -a))))
